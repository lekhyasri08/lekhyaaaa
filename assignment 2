#inlcude<stdio.h>
main()
{
int n,num,r,sum;
printf("enter 3 digit num");
scanF("%d",&n);
num=n;
while(n>0)
  {
    r=n%10;
    sum=sum+(r*r*r);
    n=num/10;
  }
    if(sum==num)
    {
    printf("armstrong");
    }
    else
    {
    printf("not an armstrong");
    }
